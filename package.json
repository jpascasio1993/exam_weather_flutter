{
  "private": true,
  "scripts": {
    "build-runner:watch": "flutter packages pub run build_runner watch",
    "build-runner:build": "flutter packages pub run build_runner build",
    "build-runner:watch:conflict": "flutter packages pub run build_runner watch --delete-conflicting-outputs",
    "build-runner:build:conflict": "flutter packages pub run build_runner build --delete-conflicting-outputs"
  }
}